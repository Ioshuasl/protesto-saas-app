export default function RegisterPage() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-semibold">Registro</h1>
      <p className="text-muted-foreground mt-2 max-w-md text-center text-sm">
        Página pública — substitua pelo fluxo real de cadastro quando o backend estiver disponível.
      </p>
    </main>
  );
}
